const config = {
  // Default Flow ID - Replace this with your actual Flow ID
  defaultFlowId: '',

  // API base URL for LangFlow
  apiBaseUrl: '/api',

  // API version
  apiVersion: 'v1',
};

export default config;